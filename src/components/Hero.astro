<section class="hero wrapper">
	<div class="hero-text">
		<div class="hero-label">QA Engineer</div>
		<h1 class="hero-name">Eduardo Macetas</h1>
		<p class="hero-sub">
			QA Automation Engineer with 8+ years of hands-on experience in both
			manual and automated testing for web and mobile applications.
			Self-driven and self-taught, I design scalable automation
			frameworks, integrate them into CI/CD pipelines, and know when
			manual testing adds more value than automation. I ensure quality
			from the first commit to production. - based in Arequipa, Peru.
		</p>
		<!-- HERO stack buttons -->
		<!-- <div class="hero-stack">
			{
				[
					"Claude Code",
					"React",
					"Next.js",
					"TypeScript",
					"Node.js",
				].map((t) => <span class="stack-chip">{t}</span>)
			}
		</div> -->
		<!-- HERO cta buttons -->
		<!-- <div class="hero-cta">
			<a href="/work/" class="btn-primary">
				View Work
			</a>
			<a href="mailto:eduardo.macetas@gmail.com" class="btn-ghost">
				Contact
			</a>
		</div> -->
	</div>
	<div class="hero-img">
		<img
			src="/assets/em.png"
			alt="Eduardo Macetas"
			width="320"
			height="400"
		/>
	</div>
</section>

<style>
	.hero {
		display: flex;
		flex-direction: column;
		gap: 2.5rem;
		padding-top: 4rem;
		padding-bottom: 3rem;
	}

	.hero-label {
		font-size: var(--text-sm);
		font-weight: 600;
		color: var(--cyan);
		letter-spacing: 0.04em;
		text-transform: uppercase;
		margin-bottom: 0.75rem;
	}

	.hero-name {
		font-size: clamp(2.5rem, 7vw, 5rem);
		font-weight: 800;
		color: var(--gray-0);
		letter-spacing: -0.04em;
		line-height: 1;
		margin-bottom: 1.25rem;
	}

	.hero-sub {
		font-size: var(--text-lg);
		color: var(--gray-400);
		line-height: 1.65;
		max-width: 46ch;
		margin-bottom: 1.5rem;
	}

	.hero-sub strong {
		color: var(--gray-200);
		font-weight: 600;
	}

	.hero-stack {
		display: flex;
		flex-wrap: wrap;
		gap: 0.375rem;
		margin-bottom: 2rem;
	}

	.stack-chip {
		background: var(--gray-800);
		color: var(--gray-300);
		border: 1px solid var(--gray-700);
		border-radius: var(--radius-sm);
		padding: 0.25rem 0.625rem;
		font-size: var(--text-sm);
		font-weight: 500;
		font-family: var(--font-mono);
	}

	.hero-cta {
		display: flex;
		gap: 0.75rem;
		flex-wrap: wrap;
	}

	.btn-primary {
		display: inline-flex;
		align-items: center;
		gap: 0.4rem;
		padding: 0.625rem 1.25rem;
		background: var(--gray-0);
		color: var(--gray-999);
		border: 1px solid var(--gray-0);
		border-radius: var(--radius-sm);
		font-size: var(--text-sm);
		font-weight: 600;
		text-decoration: none;
		transition: all var(--theme-transition);
	}

	.btn-primary:hover {
		background: var(--gray-100);
	}

	.btn-ghost {
		display: inline-flex;
		align-items: center;
		padding: 0.625rem 1.25rem;
		background: transparent;
		color: var(--gray-300);
		border: 1px solid var(--gray-700);
		border-radius: var(--radius-sm);
		font-size: var(--text-sm);
		font-weight: 500;
		text-decoration: none;
		transition: all var(--theme-transition);
	}

	.btn-ghost:hover {
		border-color: var(--gray-500);
		color: var(--gray-0);
		background: var(--gray-800);
	}

	.hero-img {
		display: none;
	}

	.hero-img img {
		width: 200px;
		height: 240px;
		object-fit: cover;
		object-position: top center;
		border-radius: var(--radius-lg);
		border: 2px solid var(--gray-700);
	}

	@media (min-width: 50em) {
		.hero {
			flex-direction: row;
			align-items: flex-start;
			justify-content: space-between;
			padding-top: 5rem;
			padding-bottom: 4rem;
			gap: 4rem;
		}

		.hero-text {
			flex: 1;
		}

		.hero-img {
			display: flex;
			flex-shrink: 0;
		}
	}

	@media (min-width: 70em) {
		.hero-img img {
			width: 300px;
			height: 375px;
		}
	}
</style>
